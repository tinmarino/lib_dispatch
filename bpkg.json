{
  "name": "lib_dispatch",
  "version": "0.0.1",
  "description": "Dispatcher utility for Bash coding",
  "scripts": [ "lib_dispatch.sh" ],
  "install": "install -b lib_dispatch.sh ${PREFIX:-$HOME/.local}/bin/lib_dispatch.sh"
}
